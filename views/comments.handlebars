{{!This page serves up the structure for the Comment webpage----}}
<div class = "container">
{{!Header with logo----}}

<nav class="navbar navbar-default-static-top">
  <div class="container-fluid">
    <div class="navbar-header">
    	<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    	<ul class="nav navbar-nav">
      		<a class="navbar-brand" href="#">
        		<img alt="Brand" src="glyphicon glyphicon-list-alt">
        		<li class="active"><a href="#">Home<span class="sr-only">(current)</span></a>
        		</li>
        		<li><a href="#">C/B</a>
        		</li>
        	</a>	
        </ul>		
      	</div>
    </div>
  </div>
</nav>

 <h3 class="text-muted">Mongo WebScraping</h3>
  </div>
  <hr>
  {{!--Display the scraped titles, links, and summary in this section.--}}
  <div class="row">
    <h1><a href="{{result.link}}" target="_blank">{{result.title}}</a></h1>
    <p>{{result.summary}}</p>
  </div>
  <div class="row">
    <h2>Previous Comments</h2>
  </div>
  {{!--If no comments exist then display the following ptag info--}}
  <div class="row">
    {{#unless result.comment}}
    <p>Be the first to comment.</p>
    {{/unless}}
  </div>
  {{!--Iterate through the comments by author and their message, display them, and have the option to delete them from the user view (also deletes from our db --}}
  <div class="row">
    <ul>
      {{#each result.comment}}
      <li style="list-style: none">
        <h5>Name: {{this.name}} </h5>
        <p>Comment: {{this.body}}</p>
        <button class="delete-button btn btn-secondary" data-comment="{{this._id}}">Delete Comment</button>
      </li>
      {{/each}}
    </ul>
    
  </div>
  <div class="row">
    <h2>Say Something About This Article</h2>
  </div>
  {{!--Do a post method for the comment(s) a user adds and save it to the db--}}
  <form method="POST" action="/articles/{{result._id}}">
    <div class="form-group row">
      <label for="name" class="col-form-label">Name</label>
      <input class="form-control" type="text" name="name" id="name" required>
    </div>
    <div class="form-group row">
      <label for="body">Message</label>
      <textarea class="form-control" id="body" name="body" rows="3" required></textarea>
    </div>
    <button class="btn btn-primary" type="submit">Submit</button>
  </form>
  <br>
  <br>
  <br>
  <br>
  <footer class="footer">
    <div class="container">
      <p>&copy; Hadi Yousufi 2017</p>
    </div>
  </footer>
</div>